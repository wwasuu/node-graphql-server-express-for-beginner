import graphql from 'graphql'
import { expect } from 'chai'
import { makeExecutableSchema } from 'graphql-tools'

import rootSchema from '../../app/graphql/rootSchema'

describe('Pokemon schema', () => {
  it('Should correct', () => {
    
  })  
});